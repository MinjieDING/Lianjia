# 链家成交数据爬虫（以广州为例）

> **Languages:** 中文 | [English](README.en.md)

基于 Selenium 的链家（Lianjia）二手房**成交数据**采集工具，以 Jupyter Notebook 形式实现。支持按区、板块批量爬取，并具备断点续爬、人机验证等待、防休眠等能力，适合长时间运行的大规模数据采集。

**本仓库以广州为例**（`gz.lianjia.com`），各城市的链家站点页面结构与爬取逻辑基本相同，更换对应城市的域名和起始 URL 即可复用到北京、上海、深圳等其他城市。

## 功能特点

- **板块列表采集**：自动遍历各行政区及其下属板块，生成板块 URL 列表（示例为广州）
- **成交数据解析**：提取小区名、户型、面积、朝向、装修、成交价、单价、挂牌价、成交日期、成交周期、楼层、建成年份、是否满五/近地铁等字段
- **断点续爬**：通过 `checkpoint.json` 与 `partial_data.json` 记录进度，中断后可从上次位置继续
- **人机验证处理**：检测到验证页时暂停并等待手动完成，完成后自动继续
- **防休眠**：后台线程定期移动鼠标，避免长时间爬取时电脑自动息屏
- **数据导出**：支持导出为 Excel（`.xlsx`）

## 项目结构

```
.
├── 广州.ipynb          # 主程序（Jupyter Notebook）
├── checkpoint.json     # 爬取进度检查点（运行中自动生成/更新）
├── partial_data.json   # 部分爬取数据（运行中自动生成）
├── 广州链家板块列表.xlsx   # 板块列表（需先运行板块采集步骤生成）
├── 临时成交数据.xlsx       # 检查点保存时的临时数据
└── 广州链家成交数据.xlsx   # 最终成交数据输出
```

## 环境要求

- Python 3.8+
- Google Chrome 浏览器
- ChromeDriver（需与 Chrome 版本匹配，Selenium 4.6+ 通常可自动管理）

### Python 依赖

```bash
pip install pandas selenium pyautogui openpyxl
```

| 包名 | 用途 |
|------|------|
| `pandas` | 数据处理与 Excel 读写 |
| `selenium` | 浏览器自动化 |
| `pyautogui` | 防休眠（鼠标微动） |
| `openpyxl` | Excel 文件写入引擎 |

## 使用说明

### 1. 克隆仓库

```bash
git clone https://github.com/MinjieDING/Lianjia.git
cd Lianjia
```

### 2. 启动 Jupyter

```bash
jupyter notebook
```

打开 `广州.ipynb`，按顺序执行单元格。

> **其他城市**：将 Notebook 中的链家域名（如 `gz.lianjia.com`）和板块起始 URL 替换为目标城市即可，例如上海为 `sh.lianjia.com`、北京为 `bj.lianjia.com`。

### 3. 采集板块列表（首次运行）

在 Notebook 中取消注释并运行「获取所有板块 URL」相关单元格，将生成 `广州链家板块列表.xlsx`，包含各区、各板块的名称与链接。

也可直接使用已有的板块列表文件，跳过此步。

### 4. 执行主爬取

运行「执行主爬」单元格，程序会：

1. 读取 `广州链家板块列表.xlsx`
2. 若存在检查点，从断点处继续
3. 逐板块、逐页爬取成交数据
4. 完成后保存为 `广州链家成交数据.xlsx`

### 5. 关闭浏览器

爬取结束或需要停止时，运行最后一个单元格关闭浏览器。

## 断点续爬

程序会在爬取过程中定期写入：

| 文件 | 说明 |
|------|------|
| `checkpoint.json` | 当前板块索引、页码、已采集条数、保存时间 |
| `partial_data.json` | 已采集的 JSON 格式原始数据 |
| `临时成交数据.xlsx` | 与检查点同步的 Excel 备份 |

中断后重新运行「执行主爬」单元格即可自动恢复。全部完成后会删除 `checkpoint.json`。

**示例检查点：**

```json
{
  "district_index": 151,
  "page_index": 10,
  "total_houses": 71020,
  "save_time": "2025-11-05 01:45:31"
}
```

## 输出字段说明

| 字段 | 说明 |
|------|------|
| `district` | 板块名称 |
| `community` | 小区名称 |
| `layout` | 户型 |
| `area` | 面积 |
| `orientation` | 朝向 |
| `decoration` | 装修情况 |
| `total_price` | 成交总价 |
| `unit_price` | 成交单价 |
| `list_price` | 挂牌价 |
| `deal_date` | 成交日期 |
| `deal_cycle_days` | 成交周期（天） |
| `floor_info` | 楼层信息 |
| `built_year` | 建成年份 |
| `position` | 位置信息 |
| `five_years` | 是否满五年 |
| `near_subway` | 是否近地铁 |
| `agent` | 经纪人信息 |
| `district_url` | 板块页面 URL |

## 账号限制与成本

实际爬取中，**每个账号大约爬取 1.5 万条记录后会被封禁**，因此大规模采集的主要成本在于**注册新手机号**以轮换账号，而非算力或带宽。

一种可行的获取手机号方式：在香港 **7-Eleven（711）便利店**购买预付费 SIM 卡，约 **30 港币/个**。

## 注意事项

1. **反爬与验证**：链家有反爬机制，频繁请求可能触发人机验证。遇到验证时请在浏览器中手动完成，程序会自动检测并继续。
2. **账号封禁**：单账号采集量有限（约 1.5 万条），需准备多个手机号注册新账号继续爬取。
3. **请求频率**：代码中已加入随机延迟（约 1.5–3.5 秒），请勿过度缩短间隔，以免被封禁。
4. **合规使用**：本项目仅供学习与研究。请遵守 [链家/贝壳](https://gz.lianjia.com) 的服务条款与 robots 规则，不得用于商业用途或大规模对外分发数据。
5. **数据时效性**：成交数据以网站实时展示为准，历史爬取结果可能随网站更新而变化。
6. **敏感信息**：Notebook 末尾含第三方短信平台链接，与爬虫逻辑无关，使用时请注意账号与隐私安全。

## 常见问题

**Q：ChromeDriver 报错怎么办？**  
A：确认 Chrome 已安装且版本与驱动匹配；或升级 Selenium 至最新版以使用自动驱动管理。

**Q：爬取中途 Notebook 崩溃怎么办？**  
A：重新打开 Notebook，从头运行初始化单元格（加载模块、启动浏览器等），再运行「执行主爬」即可从检查点恢复。

**Q：某个板块没有数据？**  
A：程序检测到「没有找到房源」时会自动跳过该板块，属正常情况。

## 许可证

本项目仅供个人学习交流使用。使用时请自行承担相关风险与责任。
